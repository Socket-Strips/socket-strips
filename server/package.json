{
  "name": "server",
  "version": "1.0.0",
  "description": "Strips socket server",
  "scripts": {
    "build": "tsc",
    "dev": "concurrently \"tsc -w\" \"nodemon dist/server.js\"",
    "lint": "eslint .",
    "seed": "tsc && node ./dist/seed.js"
  },
  "author": "RazerMoon",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^6.1.0",
    "nodemon": "^2.0.7"
  }
}
